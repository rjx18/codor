id: setup-dry-run-reports-without-mutation-or-secret
hash: a-16e6e5d32cdc
title: Setup dry-run reports exact actions without mutating or leaking
state: active
statement: Dry-run must report the complete platform action list, rendered
  service content, computed PATH, chosen access preview, and pairing-code intent
  while performing no filesystem, service-manager, readiness, or Tailscale
  effect and substituting a redaction for every token; because it is the safe
  way to inspect setup and the install proof runs it in a clean non-TTY
  container.
depends_on:
  - setup-unattended-mutation-requires-explicit-intent
  - setup-preserves-private-platform-service
created_by: codor-interactive-setup-session
