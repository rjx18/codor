// Placeholder until the ledger phase (P6).
export function LedgerPage(props: { token: string }) {
  void props;
  return (
    <main className="nx-surface" aria-label="Ledger">
      <section className="nx-surface-card">
        <h1>Ledger</h1>
        <p>The rebuilt ledger graph lands in a later build phase.</p>
        <p><a href="/">Back to the room</a></p>
      </section>
    </main>
  );
}
